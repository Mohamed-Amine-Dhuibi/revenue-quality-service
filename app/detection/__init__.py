"""Detection layer: CSV parsing, inflow classification, anomaly detection,
scoring and the orchestrating pipeline. Pure functions, no web concerns — so the
whole engine is unit-testable without an HTTP client.
"""
